-- DPM_SRC_INVENTORY.PUBLIC
-- Mirrored from the live Snowflake account via
-- GET_DDL('SCHEMA', 'DPM_SRC_INVENTORY.PUBLIC', TRUE). Generated, not hand-written:
-- re-run the dump to refresh rather than editing this file.

create or replace schema DPM_SRC_INVENTORY.PUBLIC;
