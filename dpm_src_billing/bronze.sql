-- DPM_SRC_BILLING.BRONZE
-- Mirrored from the live Snowflake account via
-- GET_DDL('SCHEMA', 'DPM_SRC_BILLING.BRONZE', TRUE). Generated, not hand-written:
-- re-run the dump to refresh rather than editing this file.

create or replace schema DPM_SRC_BILLING.BRONZE;

create or replace sequence DPM_SRC_BILLING.BRONZE.SEQ_INVOICE_ID start with 1 increment by 1 noorder;
create or replace TABLE DPM_SRC_BILLING.BRONZE.INVOICES_RAW (
	RECORD_ID NUMBER(38,0) autoincrement start 1 increment 1 noorder,
	RAW_PAYLOAD VARIANT,
	LOADED_AT TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP()
)COMMENT='Bronze: raw landed billing invoice records (append-only)'
;
create or replace stream DPM_SRC_BILLING.BRONZE.INVOICES_RAW_STREAM on table INVOICES_RAW append_only = true;
create or replace task DPM_SRC_BILLING.BRONZE.TASK_BRONZE_TO_SILVER_INVOICES
	warehouse=DPM_PIPELINE_WH
	schedule='1 MINUTE'
	when SYSTEM$STREAM_HAS_DATA('DPM_SRC_BILLING.BRONZE.INVOICES_RAW_STREAM')
	as MERGE INTO DPM_SRC_BILLING.SILVER.INVOICES tgt
USING (
  SELECT
    RAW_PAYLOAD:invoice_id::NUMBER AS INVOICE_ID,
    RAW_PAYLOAD:customer_id::NUMBER AS CUSTOMER_ID,
    RAW_PAYLOAD:amount::NUMBER(12,2) AS AMOUNT,
    RAW_PAYLOAD:status::STRING AS STATUS,
    RAW_PAYLOAD:invoice_date::DATE AS INVOICE_DATE
  FROM DPM_SRC_BILLING.BRONZE.INVOICES_RAW_STREAM
  WHERE METADATA$ACTION = 'INSERT'
) src
ON tgt.INVOICE_ID = src.INVOICE_ID
WHEN MATCHED THEN UPDATE SET
  CUSTOMER_ID = src.CUSTOMER_ID, AMOUNT = src.AMOUNT, STATUS = src.STATUS, INVOICE_DATE = src.INVOICE_DATE, UPDATED_AT = CURRENT_TIMESTAMP()
WHEN NOT MATCHED THEN INSERT (INVOICE_ID, CUSTOMER_ID, AMOUNT, STATUS, INVOICE_DATE, UPDATED_AT)
  VALUES (src.INVOICE_ID, src.CUSTOMER_ID, src.AMOUNT, src.STATUS, src.INVOICE_DATE, CURRENT_TIMESTAMP());
